after an employee finishes the questionaire, a new link is provided to see the results

the client can toggle if this functionality is active

when dprs is assigned, automatic series of messages inform over time window, links, instructions, notices and avaiablity of results

client can download reports, DSPR list, programs list

send questionario de avaliaçao for the continuous programs periodically and collect data about this for the final reports and documentation

each programa continuo has a recorded video that explains and sets the mood

add util that generates and stores contract automatically using company profile

companies can connect their google calendar

add video feature to continuous programs

Create product for employee:
develop employee platform for ai coach that is company aware (cognicare) 


filter by only the open drps in the Diagnosticos DRPS table of company workspace home, the button ver reload the page, point to the results page with data visualization, make it clear when a questionaire is open and still collecting answers or it is closed

in manager dashboars client profile Assigned processo continuos when click editar add a calendar and the events based on the program cadence set, and the dates for the events can be commited to the calendar, the calendar events are then synced to the client dashborad and his calendar, make it so only if the client wants to reschedule an event the availabilty popup show up where the availabilty appears on the calendar and the client can chose and commit. Whne this happens a notification to the manager is sent

i want to set up an internal notification system so that when a client reschedule an event or doenwloads a report a notificaiton to the manager appears in his platform

add whatzapp link to talk directly to us from the client portal

calendar filtered by clinet
tirar cargo do questionario
